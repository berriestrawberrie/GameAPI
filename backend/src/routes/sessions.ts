/**
 * Handles all /api/sessions endpoints.
 *
 * POST /api/sessions/start   → start a new session
 * POST /api/sessions/:id/stop → stop a running session
 * GET /api/sessions          → list all sessions (with filters)
 *
 * Backend team: handle session tracking logic here.
 */
